import {
  IP,
  split,
  EP,
  XOR,
  S0Box,
  S1Box,
  P4,
  merge,
  IP_1,
} from "../utils/HelperFunctions";

const SimplifiedDES = ({ plainText, keys, mode }) => {};

export default SimplifiedDES;
